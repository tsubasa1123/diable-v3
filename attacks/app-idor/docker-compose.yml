version: '3.8'

services:
  idor-lab:
    build: .
    container_name: idor-lab
    ports:
      - "8082:5000"
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
    stdin_open: true
    tty: true

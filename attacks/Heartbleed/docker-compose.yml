services:
  heartbleed:
    build: .
    ports:
      - "4443:443"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-k", "https://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
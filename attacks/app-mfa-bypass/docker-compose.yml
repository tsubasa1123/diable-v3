version: '3.8'

services:
  mfa-bypass-lab:
    build: .
    container_name: mfa-bypass-lab
    ports:
      - "8081:5000"
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
    stdin_open: true
    tty: true
